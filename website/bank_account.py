class account:
    def __init__(self,account_holder,account_name,balance=0):
        self.accuont_holder = account_holder
        self.account_name = account_name
        self.balance = balance
    def deposit(self, amount):
        self.amount = amount
        amount+=balance
    def withdraw(self, amount):
         amount = balance
    @property
    def get_balance(self):
        return current_balance = amount + balance._self 
    def __str__(self):
        print("the account balance"f"{account_holder}"and f"{account_name}")
    class saving(account):
        super.account(account_holder,account_name) 
        def __init__(self, account_number, holder_name, interest_rate, initial_balance=0)   
        self.interest_rate = interest_rate
        def add_interest(self):
            interest_rate +=balance
        def __str__(self):
        print("the account balance is equal to"f"{interest_rate}")
class CheckingAccount(Account):
    super.account(account_holder,account_name)
    def __init__(self,overdraft_limit):
        self.overdraft_limit = overdraft_limit
    def withdraw(self, amount):
            amount += self.overdraft_limit
    def __str__(self):
        print("the account balance is equal to"f"{overdraft_limit}")
acc = CheckingAccount(100,john,0,500,300)
if balance is invalid:
    raice:
    TypeError


        





         

                